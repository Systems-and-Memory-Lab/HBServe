# Capacity-relaxed all-HBM timing upper bound for the shared fixed-footprint trace.
# This changes only the address-range guard. It retains the eight-stack HBM
# timing, channel, and bandwidth model and is not a physical capacity claim.
hbm-capacity-bytes=1000000000000
